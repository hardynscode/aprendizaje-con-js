
/* 
    * El condicional else se ejecuta si la condicion impuesta es falsa

    const edad = 17

    if (edad >= 18) {
    console.log('Eres mayor de edad')
    } else {
    console.log('Eres menor de edad')
    }

    ELSE IF

    const edad = 17

    if (edad >= 18) {
    console.log('Eres mayor de edad')
    } else if (edad >= 16) {
    console.log('Eres casi mayor de edad')
    } else {
    console.log('Eres menor de edad')
    }

    /+ Condicional

    const tieneDNI = !false

    if (tieneDNI) {

        console.info("You can driving!")

    }

    Más ejemplos...


    /**
     * PODEMOS GUARDAR EL RESULTADO DE UNA CONDICION EN VARIABLES
     * 
    const edad = 17
    const tieneCarnet = true
    const puedeConducir = edad >= 18 && tieneCarnet

    if (puedeConducir) {
    console.log('Puedes conducir')
    } else {
    console.log('No puedes conducir')
    }

    Ejemplo por mí:

    let stock, clientes, productos

    clientes = true
    stocksedes = 8;
    productos = 3.1416

    validacionTienda = clientes == !false && stocksedes > 8 && productos === "number"

    if (validacionTienda) {

        console.info("Has pasado el filtro de validacion exitosamente")

    } 
    
    else if 
        console.warn("Tienes que revisar algo")
    } else {
        console.info("Debes poner mayor o igual a 8 en validacionTienda porque declaraste en la variable stocksedes que exactamente tienes 8 productos.")
    }

 */