

/* 
    * Las funciones flecha son una forma mas resumida de crear funciones en javascript ahorr mucho tiempo

    sintaxis:
    
        const saludar = () => {
            // codigo a ejecutar
        }

        //ejemplo con arrowfunction
        //ejemplo parametrizado
        const sumar = (a, b) => a + b

        //declaracion sin parametrizacion
        let S15 = () => "ZZhou Xinyu"
        let LOVElution = S15()
        
        console.log(LOVElution)

        sintaxis principal sin parametros:
        const miFuncionFlecha = () => {
        // código a ejecutar
        }


        
 */

        // Cree una funcion que borra la consola del navegador 

        /**
         * Sintaxis
         * 
         * function clearconsole (clear) {
         * 
         * console.log(clear())
         * 
         * }
         * 
         * clearconsole(clear)
         */


